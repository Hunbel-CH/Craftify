<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="<%= root_path %>">Craftify</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ms-auto">
        <% if user_signed_in? %>
          <% if current_user.buyer? %>
            <li class="nav-item <%= 'active' if current_page?(shopping_cart_path) %>">
              <%= link_to 'Cart', shopping_cart_path, class: 'nav-link' %>
            </li>
          <% end %>
          <% if current_user.artisan? %>
            <li class="nav-item <%= 'active' if current_page?(user_handcrafted_items_path(current_user.id)) %>">
              <%= link_to 'My Products', user_handcrafted_items_path(current_user.id), class: 'nav-link' %>
            </li>
          <% end %>
          <li class="nav-item">
            <%= link_to 'Edit Profile', edit_user_registration_path, class: 'nav-link' %>
          </li>
          <li class="nav-item">
            <%= link_to 'Logout', destroy_user_session_path, method: :delete, class: 'nav-link' %>
          </li>
        <% else %>
          <li class="nav-item <%= 'active' if current_page?(new_user_session_path) %>">
            <%= link_to 'Login', new_user_session_path, class: 'nav-link' %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
